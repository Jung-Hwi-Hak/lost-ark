const theme = {
	// ${({theme}) => theme.colors.컬러이름};
	colors: {
		main: '#A6FF4D',
	},

	// ${({theme}) => theme.background.컬러이름}
	background: {
		default: '#121518',
		section: '#1C1D22',
		main: '#A6FF4D',
	},
};

export default theme;
